# local data
*.pickle
*.db
*.db-*
logs/

# generated page (served by serve.py, not committed)
docs/

# secrets — never commit these
.env
github-recovery-codes.txt

# editor / macOS junk
.vscode/
.DS_Store
._*

# frontend build deps
node_modules/
